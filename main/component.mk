#
# This is the main makefile, which, from other documentation, means this is the same as a component makefile.
#
# We should include the make path for the components. Hopefully this is the right pathing I need
#

include $(IDF_PATH)/make/component_common.mk
